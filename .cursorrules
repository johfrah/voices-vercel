# Voices Headless - De Bob-methode & Chris-Protocol (2026)

Dit project volgt de **Bob-methode** (architectuur & UX) en het **Chris-Protocol** (discipline & uitvoering): een superieure, proactieve aanpak waarbij alles naadloos samenkomt in **Voices**.

## 🏛️ De Architectuur (Bob-methode)

Voices is een geünificeerd headless ecosysteem van 9 harmonieuze eenheden (**Worlds**). Elke interactie wordt geregisseerd volgens de **Atomic Architectural Trinity**:

1.  **World (De Ziel)**: De contextuele omgeving (Agency, Studio, Academy, etc.).
2.  **Market (De Etalage)**: De lokale container (domein, taal, BTW).
3.  **Journey (De Flow)**: De functionele route binnen een World/Market.

## 🛡️ De Discipline (Chris-Protocol)

De Technical Director (Chris) dwingt de wetten van de machinekamer af. Elke operatie volgt de **ID-First Handshake** en het **Stateless Integrity** mandaat.

### Kernprincipes:
- **ID-First DNA Routing**: Logica resolveert via `slug_registry` naar `entity_id`.
- **Nuclear Loading Law**: 100ms LCP via `next/dynamic` (ssr: false).
- **Zero-Slop Mandate**: Geen console errors, geen type-errors, geen data-drift.
- **Atomic Execution**: Altijd `npm run check:pre-vercel` en `forensic-audit.ts` vóór push.

## 📜 Delegatie naar Rules

Voor gedetailleerde instructies en technische wetten, raadpleeg de specifieke `.cursor/rules/`:

- `000-ATOMIC-ARCHITECTURAL-TRINITY.mdc`: De grondwet van de architectuur.
- `200-CHRIS-PROTOCOL.mdc`: De details van de uitvoering en discipline.
- `400-CONSUELA-CLEANLINESS.mdc`: Mappenstructuur en archivering.
- `400-CRUD-WORKFLOW.mdc`: Database wijzigingen en deployment.
- `800-DNA-ROUTING.mdc`: Routing en World-awareness.

## 📊 Data & Snapshots
Voor actuele database-mirrors en feitelijke informatie, raadpleeg `3-DATA/docs/`.

"Wij bouwen geen websites, wij regisseren een theater van stemmen." - Chris/Autist
