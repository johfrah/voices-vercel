---
description: 🛠️ LayoutInstruments Catalog - De officiële catalogus van UI-instrumenten. Gebruik deze in plaats van rauwe HTML.
globs: src/components/**/*.{tsx,ts}
---

# LAYOUT-INSTRUMENTS

Gebruik deze instrumenten in plaats van rauwe HTML-tags om te voldoen aan de HTML Zero wet.

## 🏗️ STRUCTURELE INSTRUMENTEN
- `<PageWrapperInstrument>`: De hoofdcontainer (`<main>`).
- `<SectionInstrument>`: Een logische sectie (`<section>`).
- `<ContainerInstrument>`: De standaard bouwsteen (`<div>`).
- `<FixedActionDockInstrument>`: Zwevende balk onderaan.

## 📝 TYPOGRAFIE INSTRUMENTEN
- `<HeadingInstrument>`: `level={1-6}`. Gebruik uitsluitend Raleway font.
- `<TextInstrument>`: `as="p"` of `as="span"`. Voor alle lopende tekst.

## 🔘 INTERACTIE INSTRUMENTEN
- `<ButtonInstrument>`: `variant="pure"`, `variant="outline"`, etc.
- `<InputInstrument>`: Gestyled volgens de Voices 2026 standaard.
- `<SelectInstrument>` / `<OptionInstrument>`: Voor dropdowns.
- `<FormInstrument>` / `<LabelInstrument>`: Voor formulieren.

## 🌍 VISUELE INSTRUMENTEN
- `<LoadingScreenInstrument>`: Full-screen loader met "Voices Wave".
- `<FlagBE>`, `<FlagNL>`, etc.: CSS-gebaseerde vlaggen.

## 📏 REGELS VOOR GEBRUIK
1. **HTML ZERO**: Gebruik van `<div>`, `<span>`, `p`, `h1-h6`, `section`, `main` is STRIKT VERBODEN.
2. **Import Mandate**: Importeer ALTIJD vanuit `@/components/ui/LayoutInstruments`.
3. **Voiceglot**: Combineer tekst-instrumenten ALTIJD met `<VoiceglotText />`.

"Een instrument is meer dan een tag; het is een belofte van kwaliteit."
