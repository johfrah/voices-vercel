---
description: 🏭 Nuclear CRUD Workflow & Data Integrity - The rules for transforming raw data into masters without hardcoding.
globs: **/*.{ts,tsx,json}
---

# 🏭 NUCLEAR CRUD WORKFLOW: DATA & CONFIG

Grondstoffen transformeren naar meesterwerken zonder hardcoding.

## 💾 DATA INTEGRITEIT (SOURCE OF TRUTH)
1.  **Database is Leading**: Tekst en config leven in Supabase, niet in lokale bestanden.
2.  **MarketManager First**: Gebruik altijd de `MarketManager` voor markt-specifieke data.
3.  **No Hardcoded Config**: Social links, contactgegevens en SEO data mogen NOOIT hardcoded in componenten staan.
4.  **Snake Case Exclusivity**: TypeScript property namen in `pgTable` moeten EXACT overeenkomen met de kolomnaam (bijv. `first_name: text("first_name")`).

## 🔄 SYNC & WORKFLOW
1.  **Nuclear Lock**: Respecteer het `is_manually_edited` veld. Overschrijf NOOIT handmatige aanpassingen.
2.  **ID-First Handshake**: Gebruik UITSLUITEND database-IDs (`uuid`) voor interne logica.
3.  **Atomic CRUD**: Data-aanpassingen gebeuren via Server Actions of API routes, ALTIJD met ID-validatie.
4.  **Sync Cleanup**: Verplaats getransformeerde Markdown bestanden direct naar `4-KELDER/CONTAINER/` (Dead Zone).

## 🧪 TYPE SAFETY
*   **No any**: Gebruik de types uit `@/types/` of genereer ze via Drizzle.
*   **Zod Validation**: Gebruik Zod voor API payload validatie om structurele integriteit te garanderen.

"Code moet dom zijn, data moet slim zijn."
