# ⚡ THE NUCLEAR WORKFLOW (2026)

**DOEL:** Een naadloze verbinding tussen de lokale cockpit (Dropbox) en de Native Engine (Combell).

---

## 1. DE ENGINE STRUCTUUR
De lokale omgeving is de enige plek waar we intelligentie toevoegen.

- **Apps:** Alle Next.js code bevindt zich in `apps/web/`.
- **Packages:** Gedeelde logica en database schema's in `packages/`.
- **Scripts:** Onderhoud en sync-tools in `scripts/`.
- **Docs:** De "Gouden Bron" van kennis en roadmaps in `docs/`.

---

## 2. 🚀 DE NUCLEAR DEPLOY WET
Deployment is een bewuste, nucleaire actie via Git.

1.  **Linting:** `npm run lint --prefix apps/web` (Verplicht).
2.  **DB Sync:** `npm run db:push` (Indien schema gewijzigd).
3.  **Git Push:** `git push origin main`.
4.  **Build:** De server bouwt de Next.js app automatisch.

---

## 3. 📥 ASSET MANAGEMENT
Assets zijn heilig en worden beheerd via de Media Engine.

- **Richting:** Assets stromen van `Dropbox -> Server` of `Client Upload -> Server`.
- **Sync:** Gebruik de `nuclear-sync` scripts om media te registreren in de database.
- **Local Dev:** Haalt media via de `/api/proxy` of direct van de productie-URL.

---

## 4. 🛠️ COMMANDO'S
- `npm run dev`: Start de lokale Freedom Machine.
- `npm run build`: Test de productie-build.
- `npm run lint`: De ultieme kwaliteitscontrole.

---
**ONTHOUD:** We bouwen een machine, geen website. Elke regel code moet de test van totale onafhankelijkheid doorstaan.
