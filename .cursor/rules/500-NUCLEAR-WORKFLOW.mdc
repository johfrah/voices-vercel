---
description: Cody's Nuclear Workflow rules for Data & Configuration in Project Atomic
globs: **/*.{ts,tsx,json}
alwaysApply: false
---

# 🏭 Cody's Nuclear Workflow: Data & Config

Grondstoffen transformeren naar meesterwerken zonder hardcoding.

## 💾 Data Integriteit

1. **Database is Leading**: Tekstuele inhoud en configuratie leven in Supabase, niet in lokale bestanden.
2. **MarketManager First**: Gebruik altijd de `MarketManager` om markt-specifieke data op te halen.
3. **No Hardcoded Config**: Social links, contactgegevens en SEO data mogen NOOIT hardcoded in componenten staan.
4. **JSONB Power**: Gebruik PostgreSQL JSONB velden voor flexibele configuraties (zoals `social_links`).

## 🔄 Sync & Seeding

1. **Nuclear Lock**: Respecteer altijd het `isManuallyEdited` veld. Overschrijf nooit handmatige aanpassingen in de database.
2. **Relative Paths**: Gebruik in `db-cli.ts` altijd relatieve paden naar het database schema (`../../../packages/database/...`).
3. **Sync Cleanup**: Verplaats getransformeerde Markdown bestanden direct naar `4-KELDER/CONTAINER/` (Dead Zone).

"Code moet dom zijn, data moet slim zijn." - Cody/Backend Techneut