# 🛡️ Architectural Lock: The Nuclear Mandate (2026)

This project follows the **Nuclear Workflow**: raw materials (Kelder) are transformed into structured data (Supabase) and then displayed via instruments (Etalage). **Hardcoding is a fatal violation.**

## 🚫 PROHIBITED ACTIONS (Zero Tolerance)

1.  **NO HARDCODED DATA**: Never embed artist bios, vision, albums, or market configurations directly in React components or API routes. Everything must live in Supabase.
2.  **NO DUPLICATE LOGIC**: Sorting and filtering must EXCLUSIVELY happen in `apps/web/src/lib/voice-filter-engine.ts`. Never re-implement sorting in local components (e.g., `AgencyContent.tsx`).
3.  **NO QUERY-PARAM JOURNEYS**: URL structure is frozen to clean paths: `/agency/[journey]/[medium]/`. Query parameters for `journey` or `media` are strictly forbidden.
4.  **NO BYPASSING IAP_CONTEXT**: Complex relational data for artists (like crowdfunding goals or album tracks) must be stored in the `iap_context` (JSONB) of the `actors` or `content_articles` tables, not in static objects.

## 🏗️ ARCHITECTURAL PILLARS (Locked)

- **Filter Motor**: `apps/web/src/lib/voice-filter-engine.ts` (Single Source of Truth for sorting).
- **URL Routing**: `apps/web/src/contexts/VoicesMasterControlContext.tsx` (Path-based routing only).
- **Nav Persistence**: `apps/web/src/components/ui/GlobalNav.tsx` (Admin-first visibility).
- **Data Source**: Supabase `actors` table + `iap_context`.

## ☢️ THE NUCLEAR CHECKLIST

Before making any change, ask:
1.  Is this data in Supabase? (If not, inject it first).
2.  Am I using the `VoiceFilterEngine`? (If not, use it).
3.  Is the URL clean? (If not, fix the router).

**Any attempt to hardcode data or logic will be rejected by the Chris-Protocol.**
