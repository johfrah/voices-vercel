---
description: Anti-AI Prose Protocol (Sonic DNA) & Backend Generation Logic. Ensures all generated text meets the Voices Tone of Voice standards.
globs: ["**/*.ts", "**/*.tsx", "**/*.md", "apps/web/src/app/api/chat/**/*"]
---

# 🚫 ANTI-AI PROTOCOL: SONIC DNA & GENERATION LOGIC

**DOEL:** Voorkom dat door AI gegenereerde tekst (content, UI-copy, Voicy-responses) herkenbaar is als "AI-generated". Dwing de "Voices Tone of Voice" af in zowel frontend als backend.

---

## 1. 🛑 DE HARDE VERBODEN (ZERO TOLERANCE)
- **Toepassing:** Deze regels gelden voor ALLE publieke teksten, UI-copy, blogposts, e-mails en Voicy-responses.
- **GEEN Em-dashes:** Gebruik NOOIT het lange gedachtenstreepje (`—`) of de dubbele dash (`--`).
- **GEEN AI-Openers:** Nooit "In de wereld van...", "Laten we...", "Stel je voor...".
- **GEEN AI-Afsluiters:** Nooit "Kortom,", "Al met al,", "In essentie,", "Concluderend,".
- **GEEN Bullet-point overkill:** Gebruik nooit meer dan 3 bullet points achter elkaar. Gebruik liever een Bento-grid structuur.

## 2. ✍️ SCHRIJFSTIJL WETTEN (SONIC RHYTHM)
- **Interpunctie:** Gebruik uitsluitend de punt (`.`), komma (`,`) en vraagteken (`?`). Vermijd het uitroepteken (`!`) tenzij het een directe CTA is.
- **Zinslengte:** Maximaal 15 woorden per zin. Knip langere zinnen doormidden.
- **Actieve Stem:** Gebruik "Wij doen" of "Jij krijgt". Vermijd lijdende vormen ("Er wordt...").
- **Woordkeuze:** Gebruik gewone mensentaal. Vervang "faciliteren" door "helpen", "optimaliseren" door "beter maken".

## 3. 🧠 BACKEND GENERATION LOGIC (SYSTEM PROMPTS)
Elke API-call naar een LLM voor publieke content MOET deze "Negative Constraints" bevatten:

```markdown
### STRIKT VERBODEN
- Geen em-dashes (—) of dubbele streepjes (--).
- Geen AI-Bingo: Cruciaal, Essentieel, Navigeren, Ontketenen, Naadloos, Robuust.
- Geen beleefdheidsruis ("Natuurlijk!", "Ik begrijp het").

### PARAMETERS
- Maximaal 15 woorden per zin.
- Informatiedichtheid: Direct naar het antwoord.
```

## 4. 🛠️ PROGRAMMATISCHE VALIDATIE
Gebruik een validatie-functie in de backend voordat content wordt opgeslagen:

```typescript
function validateVoicesProse(text: string): boolean {
  const forbidden = [/—/, /--/, /In de wereld van/i, /Kortom,/i];
  if (forbidden.some(p => p.test(text))) return false;
  if (text.split(/[.!?]/).some(s => s.trim().split(' ').length > 15)) return false;
  return true;
}
```

## 5. 🎙️ VOICY SPECIFIEK
- **Directe Start:** Geen "Natuurlijk!" of "Geen probleem.". Geef direct het antwoord.
- **Geen Herhaling:** Herhaal de vraag van de gebruiker niet.
- **Voicy Mandaat:** Voicy is UITSLUITEND de naam van onze chat-AI. Gebruik deze naam NOOIT in algemene UI-koppen.

## 6. 🏷️ OFFICIËLE BRAND NAMES (EXTERN)
Gebruik uitsluitend: **Voices Studio**, **Voices Academy**, **Ademing**, **Portfolio**, **Agency**.

---
**MANTRA:** Een machine praat in structuren. Een mens praat in beelden en acties. Wees de mens.
